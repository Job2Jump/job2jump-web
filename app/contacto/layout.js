export const metadata = {
  title: "Te llamamos — Job2Jump | Contacto",
  description: "Déjanos tus datos y te llamamos. Tanto si eres empresa buscando talento como si buscas prácticas o trabajo, estamos aquí para ayudarte.",
};

export default function ContactoLayout({ children }) {
  return children;
}
